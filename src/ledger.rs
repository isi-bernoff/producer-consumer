/// Initializes a new `Bank` with 10 `Account`s, creates `n` threads, and loads
/// `filename` with `load_ledger`
/// #Arguments
/// * `n` - The number of workers
/// * `filename` - The filename of a ledger of transactions
pub fn init_bank(n: u8, filename: String) {
}